{
  "name": "ca_firm_site3",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "deploy": "npm run build && bash -c \"git branch -D gh-pages 2>/dev/null || true && git checkout --orphan gh-pages && git rm -rf . && touch .nojekyll && cp -r out/* . && rm -rf out && git add . && git commit -m 'Deploy to GitHub Pages' && git push origin gh-pages -f && git checkout -\""
  },
  "dependencies": {
    "@emailjs/browser": "^4.4.1",
    "next": "15.4.6",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "15.4.6",
    "typescript": "^5"
  }
}
